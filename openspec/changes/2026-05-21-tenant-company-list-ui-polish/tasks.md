## Tasks

### Phase 1: 国家中文化

- [x] **T1** 扩展 `company-filters.tsx` 的国家映射，覆盖截图中出现的 `TUR`、`RUS`，并补齐主要 ISO3 国家代码。
- [x] **T2** 调整 `countryZh()` 兜底逻辑：未知 ISO3/英文值不得直接裸露给租户端用户，显示为中文兜底文案。
- [x] **T3** 检查公司列表、优选客户页、添加公司弹窗等复用 `countryZh()` 的位置，确认国家展示均为中文。

### Phase 2: 公司列表字段调整

- [x] **T4** `companies/page.tsx` 移除表头“电话”列。
- [x] **T5** `companies/page.tsx` 移除行内 `row.phone` 单元格，并同步修正空状态 `colSpan` 与表格最小宽度。
- [x] **T6** 检查 `curated-customers/page.tsx` 是否展示同类电话列；若展示，按本次列表字段要求同步移除。

### Phase 3: 页面设计优化

- [x] **T7** 优化公司列表表格视觉层级：表头、行 hover、关键列宽、截断和空值展示更稳定。
- [x] **T8** 优化操作区呈现：保留详情、群组、拉黑三个动作，但减少横向拥挤和视觉噪音。
- [x] **T9** 保持现有筛选、批量操作、分页、详情 Drawer、新增公司 Sheet 行为不变。

### Phase 4: 验证

- [x] **T10** 运行 tenant 前端匹配的 lint/typecheck/build 验证。
- [ ] **T11** 启动 tenant 前端并进行浏览器视觉检查，确认国家中文、电话列移除、页面无明显错位。（本地浏览器被登录/API 环境阻挡，仅完成登录页可达性检查；页面结构由 type-check/build 覆盖）
- [x] **T12** 更新本 change 任务状态，并在完成汇报中输出“原始需求 → 已实现/未实现”对照。
