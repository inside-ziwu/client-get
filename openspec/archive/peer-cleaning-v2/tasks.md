## 1. 合并 worktree 代码到主分支

- [ ] 1.1 将 `backend/.worktrees/codex/admin-peer-company-cleaning/` 中的 `peer_company_cleaning_service.py` 和 `peer_company_backfill_service.py` 复制到 `backend/app/services/`
- [ ] 1.2 将 worktree 中 `admin_collection_service.py` 的 peer 相关方法（`list_peer_companies`、`get_peer_company_detail`、`_peer_filter_parts`、`_format_peer_row`）合并到主分支的 `admin_collection_service.py`
- [ ] 1.3 将 worktree 中 `collection.py` 路由的 peer 端点合并到主分支的 `backend/app/api/admin/collection.py`

## 2. Migration

- [ ] 2.1 创建新 migration：建 `peer_companies` + `peer_company_keywords` + `peer_company_sources` 表（基于 worktree 的 `20260512_0040`，但移除冗余索引 `idx_peer_companies_identity`——UNIQUE 约束已提供索引）
- [ ] 2.2 在同一 migration 中新增 `peer_company_contacts` 表，注意：
  - `raw_company_id bigint REFERENCES lixiaoyun_raw_companies(id)`（不是 raw_contacts）
  - email 索引用 `lower(trim(email))` 做函数索引
  - name+phone 索引用 `COALESCE(phone, '')` 让 NULL phone 参与去重
- [ ] 2.3 在线上数据库执行 migration

## 3. 清洗规则修正

- [ ] 3.1 提取 `_build_display_set_clause()` 方法，统一 11 个字段的合并 SQL 片段（`COALESCE(NULLIF(new, ''), existing)` 最新非空覆盖）
- [ ] 3.2 `_upsert_peer_company` 和 `_update_existing_peer_display` 都使用该 helper
- [ ] 3.3 在 `clean_raw_company` 中增加 source_id 双向查找逻辑：**任何带 source_id 的 raw**（不仅限 identity_type=source_id）都先查 `peer_company_sources` 同 source_id 已有映射，有则复用该 peer

## 4. 联系人清洗

- [ ] 4.1 在 `peer_company_cleaning_service.py` 中新增 `_upsert_peer_contact` 方法：
  - **两步 upsert**：先按 (peer_company_id, source_contact_id) 查已有行，有则 UPDATE（补 email）；无则 INSERT ON CONFLICT
  - email 在插入前做 `lower(trim(email))` 规范化
  - 三级去重：email → source_contact_id → name+phone
  - 字段用 D1 相同策略（新值非空覆盖旧值）
  - 无 name 且无 email 的联系人跳过
- [ ] 4.2 修改 `clean_raw_company` 调用 `_upsert_peer_contact`，替代原有的 `_upsert_lixiaoyun_contacts`
- [ ] 4.3 修改 `refresh_peer_contact_count`：从 `COUNT(peer_company_contacts)` 计数，替代原有的跨表 DISTINCT 查询

## 5. API 对齐

- [ ] 5.1 移除 `GET /collection/peer-companies/health` 路由和对应 service 方法
- [ ] 5.2 新增 `GET /collection/peer-companies/{peer_id}/contacts` 路由，查 `peer_company_contacts`

## 6. 测试

- [ ] 6.1 新增 `backend/tests/test_peer_company_cleaning.py`，覆盖：
  - normalize_website_host（4 case）
  - derive_identity（3 case）
  - _upsert_peer_company D1 最新覆盖（3 case）
  - source_id 双向复用 D2（2 case）
  - _upsert_peer_contact 三级去重 + 升级场景 D3（5 case）
  - refresh_peer_contact_count（1 case）

## 7. Backfill

- [ ] 7.1 修改 backfill_service 适配新规则：
  - `upsert_contacts=True`（原为 False）
  - 批量事务：每 500 条 raw 一个事务，记录 last_processed_id 支持断点续传
  - runner 脚本负责循环调用 + commit 每批事务
- [ ] 7.2 线上执行 `dry_run=True` 验证数据量和去重率
- [ ] 7.3 线上执行 `dry_run=False` 正式 backfill

## 8. 前端接入

- [ ] 8.1 更新 `shared-api` 类型和 API 方法（移除 health、新增联系人端点）
- [ ] 8.2 更新 `peers-cleaned/page.tsx` 详情面板：增加联系人展示（复用同行公司页的模式）
- [ ] 8.3 构建验证 + 推送 ACR

## 9. 验证

- [ ] 9.1 线上确认清洗数据正确：peer 数 < raw 数、联系人去重、同 source_id 不分裂
- [ ] 9.2 前端页面正确展示列表、详情、联系人

## NOT in scope

- 不改 lixiaoyun_raw_companies / lixiaoyun_raw_contacts 原始表结构
- 不做 Tendata 联动
- 不做联系人端点分页（单个 peer 联系人数量小，暂不需要）
- 不做 dry_run 模拟 D2 source_id 复用（dry_run 只做粗略预估，正式 backfill 才是真正去重）
- 不做采集 worker 逻辑改造

## What already exists

| 组件 | 现状 | 复用/重建 |
|------|------|----------|
| peer_company_cleaning_service.py | worktree 完整实现，344 行 | 搬运 + 修改（D1/D2/D3/D7） |
| peer_company_backfill_service.py | worktree 完整实现，152 行 | 搬运 + 修改（batch commit） |
| admin_collection_service.py 5个peer方法 | worktree ~170 行 | 合并到主分支 |
| collection.py peer路由 | worktree ~55 行 | 合并 + 改（移除health、新增contacts） |
| migration (3表) | worktree 已有 | 不直接复制，重写含 peer_company_contacts |
| peers-cleaned/page.tsx | 主分支已有前端页面 | 修改（加联系人展示） |
| shared-api collection.ts | 主分支已有基础类型 | 修改（加联系人类型+API） |

## GSTACK REVIEW REPORT

> Generated by `/plan-eng-review` on 2026-05-14 | Branch: main | Worktree: claude/practical-bassi-edaddd

### Review Summary

| 阶段 | 发现 | 状态 |
|------|------|------|
| Scope Challenge | 范围合理，一次完成 | ✅ 通过 |
| Architecture Review | 4 issues (D3 两步upsert, D4 冗余索引, D5 批量事务, D6 FK目标) | ✅ 全部解决 |
| Code Quality Review | 1 issue (D7 DRY helper) | ✅ 已解决 |
| Test Review | 28 代码路径 0% 覆盖 → 规划 15 test cases (D8) | ✅ 已规划 |
| Performance Review | 0 issues | ✅ 通过 |
| Outside Voice (Codex) | 2 issues (D10 双向source_id, D11 email规范化) | ✅ 全部解决 |

### Decisions Log

| ID | 决策 | 选择 |
|----|------|------|
| D1 | 字段合并策略 | 最新覆盖 `COALESCE(NULLIF(new,''), existing)` — 设计文档已有 |
| D2 | source_id 复用范围 | 双向：任何带 source_id 的 raw 都查已有映射 |
| D3 | 联系人去重方式 | 两步 upsert（先查后插），解决跨层级升级 |
| D4 | idx_peer_companies_identity | 移除（UNIQUE 约束已提供索引） |
| D5 | backfill 事务粒度 | 每 500 条 raw 一个事务 + last_processed_id 断点续传 |
| D6 | peer_company_contacts.raw_company_id FK | REFERENCES lixiaoyun_raw_companies(id)（非 raw_contacts） |
| D7 | 合并 SQL 片段复用 | 提取 _build_display_set_clause() helper |
| D8 | 测试覆盖 | 15 个核心路径 test cases |
| D10 | D2 补充：双向查找条件 | 不限 identity_type，任何有 source_id 的 raw 都检查 |
| D11 | email/phone 规范化 | email 用 lower(trim())，phone 用 COALESCE(phone, '') |

### Risk Register

| 风险 | 缓解 |
|------|------|
| 条件唯一索引复杂度 | 三级去重是必要设计代价，两步 upsert 覆盖升级场景 |
| 两步 upsert 多一次查询 | 单 peer 联系人量小（<100），性能影响可忽略 |
| 最新覆盖可能覆盖好数据 | 用户已确认这是期望行为，raw id ASC = 入库时间 |
| 批量事务牺牲原子性 | last_processed_id 支持断点续传，500/批锁占时间短 |
| 12k raw backfill 期间锁竞争 | 500/批短事务 + 线上低峰执行 |

### Verdict: ✅ READY TO IMPLEMENT
